/** @jsx jsx */
import { jsx } from "theme-ui"
import Layout from "../componets/menu/layout"

export default () => (
  <Layout>
    <h1>Opps! we don't have the page that you requested</h1>
  </Layout>
)
